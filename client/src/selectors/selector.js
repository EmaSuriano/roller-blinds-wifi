export const getRollerBlindHeight = ({ reducer }) => reducer.rollerBlindHeight;

export const getNotifications = ({ reducer }) => reducer.notifications;

export const isConnected = ({ reducer }) => reducer.isSocketConnected;
